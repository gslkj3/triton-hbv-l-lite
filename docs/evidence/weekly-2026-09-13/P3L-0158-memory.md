# P3L-0158 memory

P157 establishes a nonzero safe selection but low recall: 295 strict positive
oracle requests, 228 material; seven non-Original selections, six gradeable
material positives and zero harmful adoptions; only 2.26% material log-benefit
retained.  Pipeline and phase produce zero selections.

Initial aggregate diagnosis before request-level reconstruction:

- Pipeline: 125 positive oracle coordinates are graph-replayable, but zero
  enter the P155 Cut-B terminal service because production authority qualifies
  only phase and logical routes.
- Phase: 128 graph-replayable positive coordinates; 43 reach a terminal
  decision and all 43 are numeric support misses.
- Logical: 42 graph-replayable positive coordinates; 11 reach a terminal
  decision, eight are numeric support misses and three are supported/material.

The missing 85 phase and 31 logical terminal decisions still require earliest-
stage reconstruction.  They must not be called model errors until Strong,
Weak, plan resolution, compilation and attestation are distinguished.

S method audit: P115 freezes an eight-domain funnel and risk-ranked selection;
H20 P258 retains censored winner labels and separates retrospective evaluation
from Q-derived margins; S5000 P95 derives conditional centers from Pass-owned
physical state, uses minimal coordinates/ablation, and separates
development/calibration/sealed.  These evidence methods transfer.  Their
semantics and numbers do not.

P158 request-level reconstruction is closed and independently verified:
295/295 strict L-lite-positive requests have exactly one earliest Main-L
stage.  The largest owners are 122 Bridge-composed terminal-service absences,
88 pipeline Strong-transition absences, 51 Q numeric-support misses and 21
typed nested-subject plan rejections.  These are stage/route owners, not
kernel-specific repair classes.

The first extreme-domain correctness baseline initially exposed a false
positive test: the pipeline stage 2/3/4 test created a Plan but its compile and
assertions were unreachable after a helper return.  The test was repaired at
the test owner; all three stages then compiled and reached native async-copy
artifacts.  A separate nested-loop boundary check initially assumed factor two
must eliminate a trip-four inner loop.  The Pass was correct: factor two
retains a two-group inner loop, while factor four eliminates it.  The test now
checks those two causal dispositions separately.

The online graph now constructs a complete Provider-owned pipeline subject-set
candidate for every admitted stage whenever at least two independent top-level
loops are pipeline-capable.  Membership comes only from Provider route
certificates; operator names, source locations, shapes and performance labels
are not inputs.  Strong and Weak close per member, but no terminal performance
authority is fabricated.

Under the P158 environment receipt, the isolated Main-L import ran 159 focused
legality/materialization tests successfully in 37.40 seconds.  New boundary
evidence includes Bridge-only factor 16, Bridge factor 2/16 crossed with
pipeline stage 2/4, legal Bridge/full-unroll pairs (2,2), (16,2), (16,16),
existing-loop route factors 2/16 with exact and nonzero-tail trips, nested
factor 2/4, and pipeline subject-set/nested stages 2/4.  This is compile and
artifact correctness evidence only; it makes no timing or release claim.

The first GPU runtime-equivalence sweep exposed two different owners.  Twelve
Bridge failures were a launch-harness error: the test divided the grid even
though `CompiledKernel.__getitem__` already applies the attested Bridge
divisor.  Three logical-group failures were a real compiler semantic defect:
when factor grouping retained a loop, the materializer reduced the current
group but discarded the incoming loop-carried accumulator.  That defect was
invisible in fully eliminated, zero-initialized loops and therefore explains
why the earlier compile-only evidence did not find it.

The repair remains at the generic logical-group rule.  It proves the complete
commutative `arith.addi` SSA chain, retains the incoming accumulator, and
replaces the chain with `incoming + reduce(group values)`.  It contains no
kernel, operator, source or shape dispatch.  After rebuilding with four jobs,
all 30 rule-axis GPU equivalence cases passed in 34.18 seconds, including the
previously failing retained nested loop and multiple independent subjects.
The full 159-test legality/materialization regression then passed again in
39.40 seconds.  These are correctness results, not precision performance or
safe-release evidence.

Operation-neutral runtime witnesses now cover another eight legal rule cells:
Bridge-composed phase/logical service with no load, result-bearing structured
conditionals whose values are produced inside their branches, existing phase
with pure compute or store-only bodies, and existing logical grouping with
elementwise/store or store-only bodies.  The combined runtime suite passes all
38 cells in 41.72 seconds.  A deliberately attempted logical witness whose
load result crossed a basic-block boundary was typed-refused by the existing
Provider rule; the test was corrected to the admitted branch-local-value
subdomain rather than widening the Pass.  Thus load presence is not a hidden
admission gate, while SSA-region closure remains an explicit rule boundary.

Provider-runtime full-route materialization is no longer an untested matrix
cell.  The compile Provider emits
`native_dynamic_unroll_main_ordered_remainder_v1`; phase and logical routes
were executed at runtime trips 2, 16 and 17 with route factors 2, 16 and 16.
All six exact, factor-equals-trip and nonzero-tail cases match Original, making
44/44 runtime-equivalence cases pass in 47.24 seconds.  Runtime trip values
were launch inputs only and did not admit or select the Candidate.  Pipeline
on a pure-compute loop with no native-visible asynchronous load service is
separately confirmed as a typed refusal, rather than being treated as a
full-route operation-template restriction.

W2b is now closed as a rule-partitioned matrix rather than a kernel list.  It
contains 44 legal equivalence classes with runtime equality and 12 illegal
classes with typed refusals.  The axes include Bridge identity/minimum/maximum,
route minimum/maximum, absent/static/runtime subjects, exact/tail boundaries,
focal/nested/multiple ownership, all three composition arms, and zero/nonzero
operation-service counts where each route permits them.  Interior registered
factors retain compile/attestation witnesses; relation-changing boundaries
retain runtime witnesses.  The matrix verifier passes, followed by 161/161
focused legality/materialization/attestation tests in 37.37 seconds.  This
closes Pass correctness only and does not imply a time-model or release claim.

The first real positive-key replay exposed a cross-branch Provider authority
drift rather than a time-model error.  Frozen L-lite evidence contains a
`softmax_backward_kernel_inner` BF16 stage-four pipeline candidate with strict
final materialization attestation and 2.3223x measured speedup.  Its
attestation implementation requires TTGIR async copy/commit/wait/local
service and PTX `cp.async`; it is not merely an artifact-hash difference.
Main L nevertheless refused the same two loops at TTIR because their source
elements were narrower than 32 bits, before distributed layout and per-thread
copy width existed.

P158 repairs this at the earliest owner with two-stage authority.  TTIR now
admits a top-level tensor-load candidate from a minimum source-payload fact,
while the final observer remains the sole authority for actual async service.
A deliberately under-coalesced FP16 unit case proves the negative path:
Provider admits it, final observation finds zero async/cp.async, and the route
is typed-refused with Original recovery.  The frozen real BF16 softmax
specialization proves the positive path: Main L emits 16 async copies, 16
commits, four waits, four local allocations/loads and 36 PTX `cp.async`
occurrences.  Its final SASS SHA-256 is exactly identical to the frozen L-lite
2.3223x winner.  No kernel/operator/shape predicate or performance label is
used by the repair, and the reproduction launches no GPU kernel.

Evidence roles are frozen in P158-012 (pre-repair authority drift audit) and
P158-013 (post-repair exact-artifact attestation).  The next owner is now a
fresh Strong→Weak→terminal structural replay; neither artifact is performance
calibration or a Main-L release claim.

The production Cut-B compile service now includes pipeline, but selection
authority remains deliberately absent.  Pipeline plans bind the complete
Provider-owned top-level capable set under one shared stage count, including
the valid singleton-set case; they never choose one convenient loop from a
larger set.  The contract, Strong/Weak and coarse-state layers were aligned to
this rule.  A focused 238-test suite passed, and the expanded runtime extreme
suite passed 46/46 cases, including singleton pipeline stages two and four.

P158-016/017 close the real FlagGems terminal compile service: 63 of 75
softmax-backward requests compile three pipeline candidates, yielding 189
strict final-attested terminal decisions.  All remain typed
`unqualified_rule_subdomain`, all requests retain Original, and no Candidate
launch or timing label occurs.  Thus the former terminal-service omission is
closed without fabricating a model or release authority.

P158-018 freezes the new pipeline regression protocol before new Candidate
timing.  It keeps identity-Bridge and Bridge-composed arms separate, uses a
rule-derived backend-path partition ladder, selects the minimum sufficient
feature set within a 0.005 grouped-holdout log-error equivalence band, and does
not require predicting the sign of sub-1.0175x effects.  P155 contributes only
hypotheses and failure evidence; its coefficients, support and radius are not
inherited.  P156/P157 evaluation labels cannot enter D or Q.

The Cut-B transaction now exports the current causal regression state rather
than forcing later scripts to reconstruct a shadow chain.  For each successful
pipeline Candidate it records Provider→Strong→Weak identifiable environment,
strict per-member async materialization lineage, coarse paired PTX, current
V20 and V21 typed-service coordinates.  The model tuple excludes performance,
ptxas descendants, identity coordinates, unexplained telemetry and learning
residuals.  A first real replay found and fixed an observation-schema mistake
(the named pipeline-lineage field was incorrectly treated as a positional
generic signature); the failed probe produced zero requests and zero timing
labels.

The corrected P158-020 replay passed the full FlagGems softmax-backward node:
75 requests, no timeout, about 565 seconds.  Exactly 63 requests export three
pipeline records each, for 189 records over stages two/three/four.  Every
record has the same 84-coordinate identity-free schema and all forbidden-input
flags are false.  All observed terminal paths in this population are PTX
expansion with vector destinations preserved and whole-kernel multi-member
subjects.  This is feature-export evidence, not enough path diversity to fit
or qualify a release model; the next owner is a source-template-disjoint D/Q/E
population frozen before Candidate timing.

The user subsequently changed the evidence role: frozen L-lite material-gain
operators are now permitted as targeted development data so that Main L first
demonstrates low-search recovery on those known opportunities.  They may drive
path partition, minimum-feature selection and center fitting, but cannot be
called holdout, Q, E or generalization evidence.  Operator/source/shape identity
remains forbidden as a feature, and repairs remain common causal rules rather
than per-kernel patches.  Independent Q and E are still mandatory for paper
generalization.  P158-023 records this prospective amendment before the first
new P158 fit; P158-022 collected no timing and its former D assignment is
cancelled, leaving those natural sources as Q/reserve candidates.

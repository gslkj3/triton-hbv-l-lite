# P3L-0190 memory

- P185 rejected anchor difference as an absolute-time replacement.
- Its frozen OOF results nevertheless contain strict domain-ordering gains in
  pipeline bridge-constructed-exact and logical full-elimination.
- Reuse those exact fitted model specifications only as orderers.
- P167 remains the sole absolute time head; one Candidate remains the formal
  output.

## Executed result

- Status: `P190_ANCHOR_ORDERING_CLOSED_NEGATIVE` for final material recovery.
- Two frozen orderers passed the strict domain-local gate: pipeline
  bridge-constructed-exact improved 13→14 and logical full-elimination improved
  23→25.
- Global exact-head selection improved 19/73→25/73, proving that the ordering
  role is not vacuous, but material recovery remained 40/73.
- Do not immediately add a new pairwise model.  First separate whether the
  remaining loss occurs before domain-head formation or when absolute time
  scales compete across the already formed domain heads.


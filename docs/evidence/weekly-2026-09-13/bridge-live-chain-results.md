# 单轮真实请求链运行成功，包含冷Original获取

44535注册冻结四请求、每请求6变换候选、既有模型状态与代码身份。
91853在GPU1准入/锁/持续监控下执行完成exit0。新独立缓存内，
每请求实际执行：AST准备→Original冷编译→其自身metadata/TTIR
生成Provider/强终态→计划绑定与两分支回归评分→V2至多一次候选
编译→正确性→固定32配对→原guard判定。

没有从先前score文件取得候选分数，也没有调用先前选择作为输入；
运行协议只含冻结模型状态及候选计划模板，不含参考预测或赢家。
本批4次Original编译、3次候选编译、192计时、3581事件全部审计
通过，候选plan seal/产物/Original身份一致、正确性/环境有效。

|请求|最终处置|本批点加速|条件下界|请求入口耗时|
|---|---|---:|---:|---:|
|M40N384|Original|—|—|2.03624s|
|M40N896|B4/F2条件通过|1.02752×|1.02389×|1.74848s|
|M80N384|B4/F2条件通过|1.09975×|1.09577×|1.66452s|
|M80N896|B8/F2条件通过|1.13274×|1.13022×|1.83105s|

角色从main内开始计时10.88424s，包括四请求及共享协议读取/
验证、设备准入、CUDA初始化、环境关闭等非请求时间。各阶段互不
重复累计，acquisition子阶段保留但不再次加总到请求成本。

## 尚未完成

- Python进程启动与模块导入在main前，训练/校准费用另外报告。
- 尚缺同四请求完整七选项L-lite搜索的匹配费用；不能拿旧37项
  向量化对比百分比套到此处。
- 只验证冻结固定配置的开发范围，非全部factor/家族或全域生产
  模型支持。真实统一调用入口实现了，但还不是默认发布策略。
- guard仍为有统计前提的条件结论，不写成无条件证书；本批没有
  独立全空间oracle，不跨批拼接收益恢复率。
- TopK反例及其未闭环测量责任保留。

下一步同成本边界的L-lite七选项搜索对照，冻结双方选择后独立
共同评价；不因此重新训练、改候选空间或隐藏Original费用。
全目标OPEN。证据bridge-live-chain-audit.json。

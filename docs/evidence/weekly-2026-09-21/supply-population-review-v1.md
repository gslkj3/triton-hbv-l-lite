# 供应响应修复的广覆盖重放与新增选择实测

## 全人口重放

同一保留源状态、相同阈值和选择逻辑，仅替换实验模型参数。
295入口、275有预测、5158条可预测候选均不变；非Original选择194→196。
32入口改变选择，详情supply-population-changes-v1.json。
此为旧状态池重放，不是新编译器全人口测试；276的旧缺失状态没有静默替换。
模型包supply-endpoint-response-development-v1.json仅实验，未覆盖生产模型。

## 当前编译器核验

对32个变化入口重新执行四种Bridge配置，共128组：125提取成功，256的三个
非1配置按规则拒绝。当前事实枚举905条可预测候选，940条编译规则拒绝，
2组launch不可整除；32入口均有高于门槛的选择。
每入口只编译预先选中的一条，共32/32物化及后端编译成功。
原始证据：supply-changed-facts-v1、supply-changed-predictions-v1.json、
supply-changed-selections-v1.json及supply-changed-compiled-v1。
单进程顺序编译，未超过四进程限制；85335/53803/44047均终态。

## 新增采用入口：均未取得物质收益

|入口|算子|预选候选|预测加速|实测加速|正确性|
|---|---|---|---:|---:|---|
|256|softmax_backward_kernel_non_inner|无Bridge，向量化factor2|1.0333×|0.9886×|通过|
|277|_tril_exact_row_kernel|Bridge2，重排factor2|1.2769×|0.9888×|通过|

256原始反馈v1因测试夹具使用候选SSA名arg0查绑定而失败，未产生有效收益结果。
修复为Original位置ABI绑定、逐项类型顺序检查后，v2通过；不是放宽精度或换输入。
补丁e1885acfc；失败原始结果保留。277使用已支持位置ABI的scalar夹具通过。
GPU0共享允许的开发计时，环境头留证；结果不能当精密系数或严格全局结果。
会话29666/38495终态，无GPU活任务。

## 当前判断与后续责任

276已知输入及新增批次的局部恢复有效，但新增277未验证这种收益迁移；不能用
32条编译成功或选择数增加来宣称收益改善。生产模型不部署。
277比256有更大方向性预测错误，下一步优先对276/277的预编译每块服务状态、
总供应、资源/指令进行成对分析，识别是否被当前状态近似混同；不按算子拆head。
其余30个变化入口已有物化产物，尚需对应选择的新执行反馈或精确身份绑定的旧证据。
正式全局27/228不变；没有将局部通过计入正式分子。

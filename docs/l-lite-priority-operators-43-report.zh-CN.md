# L-lite 43 个重点算子测试与候选缺口报告

> 测试日期：2026-08-27  
> 结果等级：共享 GPU（图形处理器）开发级测量；允许其他负载带来的误差  
> 编译器规则：测试开始后冻结；没有按本批性能结果修改候选规则  
> 结论状态：43 个算子全部获得终态，旧聚合账本另用 17 个角色重跑补齐到逐 Route（优化路线）证据

冻结证据哈希如下：76 角色主状态为
`db2c998cc6a4a9527224fa210397e696a28c648b1560e6681a56d616adf6cb75`，17 角色缺口重跑状态为
`9928518082423b609da3a3ee4737ad6028995456ce64fae72b5fc6379d848552`，最终 V62（第 62 版）
逐请求候选归因结果为
`ea65b1dd8a65cca136caa01ce4da65242b2a2c0524de6596fa8a441af8a18904`。

## 1. 这批测试回答什么问题

本批测试覆盖评审指定的 43 个重点算子。每个算子都沿同一条链检查：

```text
发现 Bridge（桥接构环）或原生循环机会
→ 枚举 Bridge factor（桥接因子）与 Route factor（路线因子）
→ 编译候选
→ 验证候选是否真正物化
→ 验证数值正确性
→ 交给 autotune（自动实测选优）计时
→ 在 Original（原始编译路径）与全部可执行候选中选最快者
```

这里必须区分三种结果。

第一，候选形成且被选中，说明当前算子和输入上找到了开发级收益点。

第二，候选形成但 Original 获胜，说明变换在语义和工程上可用，只是在该输入上不经济。这不是
机制缺陷，也不应为了让报表更好看而强行选择优化候选。

第三，发现了结构机会但没有形成可执行候选。此时继续追到最早断点，判断它是正常因子拒绝、
尚未闭合的证明、Bridge 与 Route 的组合能力缺口、机会判断过宽，还是候选构造与最终物化之间
的真实契约缺陷。

`geometric mean`（几何平均）把同一算子的所有已完成请求一起统计；其中 Original 获胜的请求按
1.0× 计入。`acquisition`（首次候选获取）包括候选编译、无效候选处理和本地 autotune 时间，
不是只计算最后获胜候选的运行时间。

## 2. 总结

本轮最终形成 1057 个可计时 kernel（内核）请求，328 个请求选择非 Original，13 个算子至少
找到一个优化胜者：

- 13 个算子找到收益；
- 1 个算子形成合法候选，但全部由 Original 获胜；
- 12 个算子完成实测，但没有优化胜者，或全部优化候选被类型化拒绝；
- 5 个算子在超时前已经生成候选机器码，收益仍未决；
- 3 个算子超时，但旧证据不足以证明具体候选机器码范围；
- 9 个算子被当前 SM89（NVIDIA Ada 架构能力 8.9）、参考实现或依赖条件阻断，未形成 L-lite
  可计时请求。

这批结果证明 L-lite 能在自然算子上找到明显收益，但也再次证明完整笛卡尔积 autotune 的首次
成本很高。例如 `grouped_topk` 的聚合收益为 1.0509×，首次候选获取却累计 4188.82 秒；它适合
充当能力上限和实验对照，不适合作为所有生产 kernel 的默认决策器。

## 3. 找到收益的算子

| 算子 | 请求/优化胜出 | 主要获胜路线 | 全部请求几何平均 | 最大加速 | 首次候选获取 |
|---|---:|---|---:|---:|---:|
| `cp_gather_indexer_k_quant_cache` | 4/4 | 完全展开加重排 | 1.3398× | 1.7255× | 111.10 秒 |
| `apply_rotary_pos_emb` | 30/30 | 完全展开加向量化 | 1.1685× | 1.4400× | 907.23 秒 |
| `gemma_rms_norm_kernel` | 6/3 | 重排 2 项、软件流水 1 项 | 1.0655× | 1.2000× | 182.35 秒 |
| `grouped_topk` | 324/135 | 向量化 69、重排 65、软件流水 1 | 1.0509× | 1.5000× | 4188.82 秒 |
| `pack_seq_triton` | 36/25 | 软件流水 17、向量化 7、重排 1 | 1.0502× | 1.2857× | 320.18 秒 |
| `flash_attn_varlen_func` | 16/9 | 软件流水 | 1.0309× | 1.1348× | 344.34 秒 |
| `bmm` | 144/20 | 软件流水 10、重排 10 | 1.0243× | 2.8506× | 716.45 秒 |
| `moe_sum` | 120/69 | 重排 58、软件流水 6、向量化 5 | 1.0227× | 1.1818× | 1208.79 秒 |
| `group_mm` | 15/6 | 软件流水 | 1.0153× | 1.1571× | 82.95 秒 |
| `unpack_seq_triton` | 36/7 | 完全展开加向量化 | 1.0144× | 1.2800× | 29.07 秒 |
| `chunk_gated_delta_rule_fwd` | 82/16 | 软件流水 | 1.0077× | 1.1429× | 692.35 秒 |
| `top_k_per_row_prefill` | 13/2 | 完全展开加向量化 | 1.0061× | 1.0458× | 188.99 秒 |
| `swiglu` | 12/2 | 软件流水 | 1.0052× | 1.0549× | 227.81 秒 |

这些是共享 GPU 上的开发级点值，不能直接作为论文最终收益下界。特别是 `bmm` 的 2.8506×
最大值仍需独占环境复核；本报告保留它，但不把单个最大值当作整体收益。

`swiglu` 的原测试依赖 TransformerEngine（英伟达 Transformer 加速库），冻结环境缺少该参考
库，所以第一轮被跳过。补测使用独立 PyTorch（张量计算框架）表达式验证正确性，并把原仓库
的真实 wrapper（调用封装）放入 Triton 正常计时入口。两库各 6 个请求全部通过，均形成候选；
FlagGems-vLLM 的一个请求由软件流水候选以约 1.055× 获胜。

`gemma_rms_norm_kernel` 第一版适配器虽然 6/6 正确，却在计时入口之前填充了 FlagGems
`LibEntry`（库入口）的上层编译缓存，导致 L-lite 没有接管。第二版只调整测试入口顺序：先在
正常计时作用域内触发首次编译，再用独立 PyTorch 结果检查数值。补测得到 6 个真实请求，3 个
选择优化，整体 1.0655×。这个过程没有修改编译器准入或候选实现。

## 4. “发现机会但没形成候选”如何判定

“机会”不是单一布尔值。报告把它拆成三层：

1. 结构机会：IR（中间表示）中看到了循环、可构造 Bridge 对象或可能参与某条 Route 的主体；
2. 已证明合法机会：具体 subject（作用对象）、factor、依赖、尾部和组合证书全部闭合；
3. 可执行候选：候选完成物化、后置身份验证、机器码生成和数值正确性检查。

只有第二层已经成立、第三层仍因实现契约断裂而失败，才能直接叫“候选机制缺陷”。第一层到
第二层失败，可能只是证明覆盖不足或该结构根本不适用于当前 Route。这个区分防止把安全拒绝
误报成缺功能，也防止用“只是拒绝”掩盖真实的上下游契约错误。

### 4.1 Bridge 自身没有发现具体因子候选形成缺陷

在保存了新式逐候选账本的请求中，只要 Bridge 存在具体合法 factor，均生成了 Bridge 候选。
没有出现“factor 2/4/8/16 已被证明合法，但一个 Bridge 候选都没形成”的情况。

另有 44 个请求在静态层面允许考虑 Bridge，但当前 launch grid（启动网格）上不存在可用 factor：
`fused_experts_impl` 16 个、`grouped_topk` 12 个、`flash_attn_varlen_func` 8 个、`swiglu` 8 个。
典型情况是被分组轴长度为 1，或没有任何允许的 2/4/8/16 因子能构成完整分组。这里不生成候选
是正确行为，不是替 autotune 偷做性能筛选，也不是 Bridge 物化缺陷。

### 4.2 已确认的候选契约缺陷

下表的“单元”指一个具体 kernel 请求与一条具体 Route 的组合。同一请求可以同时暴露重排和
向量化问题，所以不能把单元数理解成独立 kernel 数。

| 缺陷 | 涉及算子 | 单元数 | 最早断点与影响 |
|---|---|---:|---|
| 候选参数到最终准入契约不一致 | `bmm`、`combine_topk_swa_indices`、`compute_global_topk_indices_and_lens`、`dequantize_and_gather_k_cache`、`fused_experts_impl`、`group_mm`、`moe_sum`、`sparse_attn_triton` | 236 | Python 候选层提交了软件流水组合，但最终 Loop V2 解析器判定 subject/factor 准入字段不完整或互斥条件错误；这是真实的候选构造契约缺陷 |
| 规划作用对象到最终物化身份漂移 | `group_mm`、`grouped_topk` | 20 | 规划阶段保存了现有循环作用对象，进入最终物化时已找不到对应的 fresh eligible `scf.for`（仍可作用的结构化循环）；需要统一 planning cut（规划切点）和最终 identity（身份） |
| 软件流水作用对象集合不完整 | `top_k_per_row_decode`、`top_k_per_row_prefill` | 16 | Plan（计划）绑定的软件流水集合没有覆盖最终事实中全部 capable loop（可流水循环）；为避免只优化一部分却声称完整物化，后置验证拒绝候选 |

这三类合计影响 272 个“请求 × Route”单元，涉及 11 个算子。它们不会污染正确性，因为最终
验证已经拒绝了候选；但它们会缩小可执行候选空间，因此属于主 L 与 L-lite 后续共享
CandidateCompiler（候选编译器）必须修复的机制问题。

### 4.3 证明或组合能力尚未闭合

| 类型 | 涉及范围 | 单元数 | 当前结论 |
|---|---|---:|---|
| 动态 main/tail（主体/尾部）证书缺失 | 10 个算子 | 486 | 看到了运行期循环，但完全展开路线需要证明主段按 factor 分组、尾部保持原语义；证书缺失时拒绝是安全行为。它是动态循环证明覆盖缺口，不等于物化器随机失败 |
| Bridge 与软件流水组合缺口 | `moe_sum` | 42 | Bridge 已构造外层程序循环，但其内部找不到唯一的原生可流水主体。当前实现明确不把新构造的外层循环直接当作原生流水主体；若要支持，必须先证明该循环具有独立加载/计算阶段，不能简单删门 |

这两类共 528 个单元。它们可能包含未来收益机会，但当前证据不足以把它们叫作“本来合法却
漏生成候选”。尤其是动态 main/tail，补功能必须先补证明，而不是跳过检查把未知候选交给
autotune。

### 4.4 机会判断比最终可物化条件更宽

| 类型 | 算子 | 单元数 | 解释 |
|---|---|---:|---|
| 软件流水机会没有产生所需 artifact（最终产物事实） | `apply_rotary_pos_emb` | 20 | TTIR 中有循环且原生流水参数可提交，但最终机器码没有形成要求的异步拷贝/提交/等待事实；候选被正确拒绝。需要收紧“可流水”机会定义，而不是接受无效果候选 |
| 向量化机会没有精确可打包操作组 | `flash_attn_varlen_func`、`moe_sum` | 50 | 循环存在，但该 factor 下找不到能保持地址、mask（掩码）、副作用和 SSA 依赖的对应操作组；属于机会资格与物化前置条件不一致 |

这 70 个单元揭示的是 Strong opportunity（强机会判断）过宽，不是最终验证器太严格。正确的
修复位置在机会事实或 Route 准入规则，不能给具体算子加白名单，也不能降低后置证明标准。

### 4.5 正常拒绝不是机制缺陷

60 个单元至少包含“factor 大于精确 trip count（循环次数）”这一正常拒绝，分布在
`flash_attn_varlen_func`、`group_mm`、`grouped_topk` 和 `moe_sum`。候选空间按完整笛卡尔积
生成时，这些无效组合仍进入账本并产生处理开销；它们不会被执行，也不能算作缺功能。

共享内存或寄存器资源超限同样属于候选编译后的硬件限制。L-lite 不在提交前用自己的性能
知识替 autotune 缩小空间，但候选无法生成合法机器码时仍必须标成无效，不能真的执行。

## 5. 超时结果

超时是允许的终态，但不能被写成 1.0×，也不能凭缓存文件宣称有收益。下列 5 个算子已经有
候选 metadata（元数据）和 cubin（GPU 二进制）持续生成，证明超时来自候选获取规模，而非
测试进程死循环：

| 算子 | 已观察候选机器码 | 覆盖情况 | 结论 |
|---|---:|---|---|
| `chunk_gla` | 606 | Bridge 2/4/8；三条 Route 均有产物 | 物化存在，900 秒内未完成收益选择 |
| `chunk_gdn2` | 144 | Bridge 2/4/8；三条 Route 均有产物 | 物化存在，900 秒内未完成收益选择 |
| `add_rms_norm` | 578（两库合计） | Bridge 2/4/8/16；三条 Route 均有产物 | 物化存在，两个角色均超时 |
| `addmm` | 133 | Bridge 2/4/8/16；三条 Route 均有产物 | 物化存在，收益未决 |
| `rms_norm` | 486 | Bridge 2/4/8/16；三条 Route 均有产物 | 物化存在，收益未决 |

`baddbmm`、`conv2d`、`topk` 也达到 900 秒边界，但复用的旧角色没有保存同等粒度的隔离缓存，
因此只能报告“没有完整收益结论”，不能补写具体候选覆盖。

另外三个算子是按角色而非按算子整体出现混合结果：`chunk_gated_delta_rule_fwd` 的 FlagGems
角色找到收益而 FlagGems-vLLM 角色超时；`cp_gather_indexer_k_quant_cache` 的 FlagGems-vLLM
角色找到收益而 FlagGems 角色超时；`compute_global_topk_indices_and_lens` 的 FlagGems-vLLM
角色完成无收益结论而 FlagGems 角色超时。总表按已经获得的可计时请求统计收益，同时保留这些
未完成角色，不用成功仓库覆盖失败仓库。

## 6. 被环境或参考实现阻断的算子

| 算子 | 阻断位置 | 是否属于 L-lite 机制缺陷 |
|---|---|---|
| `flash_mla_with_kvcache` | 当前 SM89 不支持参考实现 `sparse_decode_fwd` | 否；在 L-lite 前失败 |
| `fp8_einsum` | 基准要求 DeepGEMM（深度矩阵乘库）、Hopper 和 Triton 3.6 以上能力组合 | 否；当前硬件不满足 |
| `fused_inv_rope_fp8_quant` | 要求原生 FP8（8 位浮点）与可用 vLLM 参考实现 | 否；冻结环境不满足 |
| `fused_marlin_moe` | 要求 vLLM Marlin（量化矩阵乘实现）与 Hopper | 否；当前 SM89 不满足 |
| `fused_marlin_moe_int8` | 同上 | 否 |
| `fused_marlin_moe_mxfp4` | 同上 | 否 |
| `megamoe` | 两个角色分别要求 Blackwell SM100+ 或原生 SM90+ FP8 | 否；当前 SM89 不满足 |
| `persistent_topk` | Triton language extension（Triton 语言扩展）未初始化，`tle.gpu.alloc` 在编译前报错 | 不是候选机制缺陷；属于依赖/运行环境缺口 |
| `router_gemm` | 成对工作负载在动态函数参数或共享内存限制处先失败 | 不是已证明的候选机制缺陷；尚无可计时请求 |

`fused_add_rms_norm` 和 `fused_deepseek_v4_qnorm_rope_kv_rope_quant_insert` 不在此表，因为它们在
发生 GPU 内存不足之前已经分别完成 12 和 18 个请求。这些已完成请求均由 Original 获胜；
后续超大输入的 OOM（显存不足）仍保留为角色失败原因。`fused_experts_impl` 也保留了 42 个
请求和候选拒绝账本，但测试最终发生成对正确性/依赖失败，所以不能形成收益声明。

## 7. 43 个算子的完整结果

| 算子 | 请求 | 优化胜出 | 全部请求几何平均 | 最大加速 | 首次候选获取 | 终态 |
|---|---:|---:|---:|---:|---:|---|
| `add_rms_norm` | 0 | 0 | — | — | — | 超时；候选已物化，收益未决 |
| `addmm` | 0 | 0 | — | — | — | 超时；候选已物化，收益未决 |
| `apply_rotary_pos_emb` | 30 | 30 | 1.1685× | 1.4400× | 907.23 秒 | 找到收益；软件流水另有机会资格过宽 |
| `baddbmm` | 0 | 0 | — | — | — | 超时；无完整收益结论 |
| `bmm` | 144 | 20 | 1.0243× | 2.8506× | 716.45 秒 | 找到收益；动态证明和候选准入契约仍有缺口 |
| `chunk_gated_delta_rule_fwd` | 82 | 16 | 1.0077× | 1.1429× | 692.35 秒 | FlagGems 找到收益；另一仓库角色超时 |
| `chunk_gdn2` | 0 | 0 | — | — | — | 超时；144 个候选机器码已形成 |
| `chunk_gla` | 0 | 0 | — | — | — | 超时；606 个候选机器码已形成 |
| `combine_topk_swa_indices` | 14 | 0 | 1.0000× | 1.0000× | 9.73 秒 | 无优化胜者；动态证明与准入契约缺口 |
| `compute_global_topk_indices_and_lens` | 5 | 0 | 1.0000× | 1.0000× | 2.83 秒 | vLLM 完成无收益；FlagGems 超时 |
| `conv2d` | 0 | 0 | — | — | — | 超时；无完整收益结论 |
| `cp_gather_indexer_k_quant_cache` | 4 | 4 | 1.3398× | 1.7255× | 111.10 秒 | vLLM 找到收益；FlagGems 超时 |
| `dequantize_and_gather_k_cache` | 21 | 0 | 1.0000× | 1.0000× | 17.90 秒 | 无优化胜者；动态证明与准入契约缺口 |
| `flash_attn_varlen_func` | 16 | 9 | 1.0309× | 1.1348× | 344.34 秒 | 找到收益；部分向量化主体不可精确打包 |
| `flash_mla_with_kvcache` | 0 | 0 | — | — | — | SM89 不支持参考实现 |
| `fp8_einsum` | 0 | 0 | — | — | — | Hopper/DeepGEMM 条件不满足 |
| `fused_add_rms_norm` | 12 | 0 | 1.0000× | 1.0000× | 9.37 秒 | 小中型输入完成；后续超大输入 OOM |
| `fused_deepseek_v4_qnorm_rope_kv_rope_insert` | 5 | 0 | 1.0000× | 1.0000× | 3.84 秒 | 完成；无优化胜者 |
| `fused_deepseek_v4_qnorm_rope_kv_rope_quant_insert` | 18 | 0 | 1.0000× | 1.0000× | 19.19 秒 | PR684 路径完成 18 个请求；后续 OOM |
| `fused_experts_impl` | 42 | 0 | 1.0000× | 1.0000× | 32.58 秒 | 候选账本完成；测试/正确性角色失败 |
| `fused_inv_rope_fp8_quant` | 0 | 0 | — | — | — | 原生 FP8/参考实现条件不满足 |
| `fused_marlin_moe` | 0 | 0 | — | — | — | Hopper/Marlin 条件不满足 |
| `fused_marlin_moe_int8` | 0 | 0 | — | — | — | Hopper/Marlin 条件不满足 |
| `fused_marlin_moe_mxfp4` | 0 | 0 | — | — | — | Hopper/Marlin 条件不满足 |
| `fused_q_kv_rmsnorm` | 14 | 0 | 1.0000× | 1.0000× | 10.79 秒 | 完成；无已观察循环机会 |
| `gemma_rms_norm_kernel` | 6 | 3 | 1.0655× | 1.2000× | 182.35 秒 | 补正计时入口后找到收益 |
| `glu` | 15 | 0 | 1.0000× | 1.0000× | 11.17 秒 | 完成；无优化胜者 |
| `group_mm` | 15 | 6 | 1.0153× | 1.1571× | 82.95 秒 | 找到收益；部分作用对象身份契约未闭合 |
| `grouped_topk` | 324 | 135 | 1.0509× | 1.5000× | 4188.82 秒 | 两库、多输入均找到收益；获取成本最高 |
| `indexer_k_quant_and_cache` | 44 | 0 | 1.0000× | 1.0000× | 21.64 秒 | 完成；无已观察循环机会 |
| `megamoe` | 0 | 0 | — | — | — | SM90/SM100 能力条件不满足 |
| `moe_sum` | 120 | 69 | 1.0227× | 1.1818× | 1208.79 秒 | 找到收益；Bridge×流水与动态证明仍有缺口 |
| `pack_seq_triton` | 36 | 25 | 1.0502× | 1.2857× | 320.18 秒 | 两库均找到收益 |
| `per_token_group_quant_fp8` | 10 | 0 | 1.0000× | 1.0000× | 201.51 秒 | 合法候选形成，但 Original 全部获胜 |
| `persistent_topk` | 0 | 0 | — | — | — | `tle.gpu` 依赖未初始化 |
| `rms_norm` | 0 | 0 | — | — | — | 超时；486 个候选机器码已形成 |
| `router_gemm` | 0 | 0 | — | — | — | 成对工作负载先失败 |
| `sparse_attn_triton` | 3 | 0 | 1.0000× | 1.0000× | 4.51 秒 | 无优化胜者；动态证明与准入契约缺口 |
| `swiglu` | 12 | 2 | 1.0052× | 1.0549× | 227.81 秒 | 独立参考补测后找到小幅收益 |
| `top_k_per_row_decode` | 16 | 0 | 1.0000× | 1.0000× | 115.96 秒 | 无优化胜者；软件流水集合契约未闭合 |
| `top_k_per_row_prefill` | 13 | 2 | 1.0061× | 1.0458× | 188.99 秒 | vLLM 找到收益；部分流水集合契约未闭合 |
| `topk` | 0 | 0 | — | — | — | 超时；无完整收益结论 |
| `unpack_seq_triton` | 36 | 7 | 1.0144× | 1.2800× | 29.07 秒 | vLLM 找到收益；FlagGems 由 Original 获胜 |

## 8. 对主 L 的直接含义

这批测试没有改变主 L 的目标。合法性、物化、artifact 和正确性仍应由主 L 与 L-lite 共享；
L-lite 用完整实测给出候选上限，主 L 用论文可信的因果模型和收益下界减少 acquisition。

在把这些结果用于主 L 的论文级预测前，应先处理三类已确认的共享 CandidateCompiler 契约
缺陷，并决定动态 main/tail 与 Bridge×软件流水是否扩展证明能力。不能让主 L 的回归模型把
“候选根本没有形成”学习成性能差，也不能用时间残差吸收候选身份错误。

本轮同时给出了两个适合主 L 验证的强正例：`apply_rotary_pos_emb` 的向量化在 30/30 请求上
获胜，`grouped_topk` 在两库和多种模式上重复找到收益。也给出了强负例：
`per_token_group_quant_fp8` 有合法候选但 Original 全胜。三者适合分别验证“稳定正收益”、
“跨仓库可复现收益”和“合法但不经济”的预测边界。

## 9. 2026-08-28：缺陷修复、受影响角色重测与主 L 对齐

### 9.1 如何阅读这一节

前面第 7 节是首次冻结的 43 算子结果。它回答“初始版本在既定测试集上发生了什么”，不能
自动代表修复后的最终源码。开发过程中确认了 272 个 CandidateCompiler（候选编译器）缺陷
单元；修复这些缺陷后，如果只保留旧表，就会把已经消失的拒绝继续写成当前能力。因此本节
另行记录最终共享源码下的回归，不修改或覆盖原始结果。

这里有两个不同的“完成”：

1. **角色终态完成**：测试进程已经结束，并留下完整通过、类型化失败、跳过或超时中的一种
   明确终态；
2. **性能测量完成**：该角色的全部测试请求、候选编译、正确性检查和原生 autotune 均在时间
   边界内闭合，可以发布收益数字。

本轮 20 个受影响角色全部取得角色终态，但只有 5 个取得完整性能测量。其余 15 个不是“测试
还在运行”，而是未剪枝笛卡尔积在每角色 120 秒边界内没有完成；它们只发布已经落盘的候选级
合法性和物化证据，不发布收益。

### 9.2 确认并修复的功能缺陷

| 缺陷 | 根因 | 规则式修复 | 修复后证据 |
|---|---|---|---|
| V2 作用对象与 factor 不一致 | Bridge factor 为 1 时，计划沿用了调用侧推测的循环主体和长度，没有绑定 Provider 在同一规划切点观测到的真实主体 | 由 Provider 事实唯一生成 route subject、exact trip 或运行期 main/tail 证书；缺少证书的运行期主体不能进入 V2 | `bmm` 代表回放中 81 个候选通过资格；最终回放到达的坐标中旧错误为 0 次 |
| 规划身份到物化身份漂移 | 计划保存的是旧 eligible loop 身份，最终物化阶段要求另一份 fresh loop 身份 | 规划、契约、物化和后置验证统一使用不可变 Provider locator 与 capability certificate | `group_mm`、`grouped_topk` 等最终回放不再出现 “fresh eligible existing scf.for” 错误 |
| 软件流水主体集合不完整 | 一个 kernel 中有多个直接可流水主体或经证明的嵌套主体，旧计划只绑定其中一部分 | 软件流水集合由 Provider 枚举全部直接 capable loop；嵌套成员必须没有伪造父子上下文，并在最终 IR 重算真实 nesting depth（嵌套深度） | `top_k_per_row_decode` 完整通过；旧“没有命名全部 capable loop”错误为 0 次 |
| Bridge 后 logical-group 的机会判定晚于物化失败 | 规划阶段只看到“有循环”，没有先证明分组 load 的边界、padding（填充）、使用块和 lineage（来源链）可精确打包 | 增加 prospective grouped-load lineage certificate（前瞻式分组加载来源证书）；它不要求必须存在 load，因此 store-only 和通用逐元素结构不会被隐性排除 | 无法打包的组合在规划/资格阶段得到类型化原因；可打包组合继续进入真实物化 |
| `scf.while` 未按原生流水 outer-loop 规则处理 | 旧能力判定只把嵌套 `scf.for` 当作内层循环容器 | 与原生 pipeliner 的 outer-loop 判定一致，同时识别 `scf.for` 和 `scf.while` | 两侧 MLIR 定向测试均通过 7/7 |
| `fused_experts_impl` 非法显存访问 | 编译观察器使用 `warmup=False`，事实提取阶段暗中执行了一次 Original；带 atomic（原子写）或同一根参数读写的多阶段 kernel 状态被提前改变，后续 stage 使用被放大的状态越界访问 | 观察器改为只编译并加载 metadata（元数据），不启动 device kernel；若没有 Triton 标准 reset/restore 协议且检测到 atomic 或同根读写，禁止嵌套重复 autotune，类型化退回 Original 单次执行。规则不含算子名或源码白名单 | 两库原生基线均通过；直接子系统测试各 12 个请求通过，其中 11 个得到类型化安全旁路；四个 `fused_experts_impl` 回放均越过旧故障切点，非法访问 0 次复现 |

最后一项不是 Bridge/Route 等价性证明错误，而是候选获取事务破坏了有状态 workload（工作负载）
的执行前提。它必须在 autotune 获取层解决，不能把非法访问归到某条 Route，也不能用“候选性能
差”掩盖。

### 9.3 最终共享源码下的受影响角色回归

测试固定使用串行角色、最多 4 个编译进程、每角色 120 秒。GPU 允许共享，因此下面的收益是
开发期结果，不是高精度独占发布值；合法性、物化、正确性和缺陷复现结论不依赖把性能噪声
解释为收益。

| 仓库与角色 | 请求 | 优化胜出 | 几何平均 | 首次候选获取 | 终态 |
|---|---:|---:|---:|---:|---|
| FlagGems-vLLM `top_k_per_row_decode` | 5 | 0 | 1.0000× | 33.57 秒 | 完整通过 |
| FlagGems `combine_topk_swa_indices` | 7 | 7 | 1.2602× | 72.73 秒 | 完整通过 |
| FlagGems-vLLM `combine_topk_swa_indices` | 7 | 7 | 1.2864× | 65.97 秒 | 完整通过 |
| FlagGems-vLLM `compute_global_topk_indices_and_lens` | 5 | 4 | 1.0473× | 60.74 秒 | 完整通过 |
| FlagGems `top_k_per_row_prefill` | 5 | 0 | 1.0000× | 42.62 秒 | 完整通过 |
| 其余 15 个受影响角色 | — | — | — | 至少 120 秒/角色 | 候选级证据已保存，穷举获取超时，不发布收益 |

两个 `combine_topk_swa_indices` 角色尤其重要。初始快照因为 CandidateCompiler 契约缺陷没有
优化胜者；修复后，两库的 14 个请求全部选择合法非 Original 路线，分别得到约 1.260× 和
1.286× 的开发期几何平均。这是“功能缺陷确实缩小候选空间”的直接反事实证据，而不是仅靠
单元测试推断修复有效。

15 个超时角色中，候选进度文件仍记录了实际跨过原故障点的物化资格。例如最终回放中，
`group_mm` 有 20 个、两库 `dequantize_and_gather_k_cache` 分别有 51 和 32 个、四个
`fused_experts_impl` 角色各有 142 至 153 个候选通过资格。三类历史缺陷签名和非法显存访问
均为 0 次复现。由于超时发生在未剪枝 native autotune（原生自动调优）的获取阶段，这些数字
证明“修复后的路径能够继续”，但不证明所有 272 个历史坐标都在时限内逐一到达。

### 9.4 与主 L 的对齐边界

以下三份共享源码已在 L-lite 开发树、L-lite 实际构建树和主 L 树中做到字节级一致：

| 共享责任 | 文件 | SHA-256 |
|---|---|---|
| Plan/契约合法性 | `python/triton/hbv/_contract.py` | `da24e8821cd5da987c47460fccee4e18791afb98c30cc606b458bd008d361de4` |
| Provider 作用对象绑定 | `python/triton/hbv/mechanistic/_loop_sequential_provider_subject.py` | `692f2bc14fd072820a6eabd8a2f83cb70f486130cb1a581a9ea22f1e51f4026d` |
| Bridge、Route 物化与最终后置验证 | `lib/Dialect/Triton/Transforms/HBVLoop.cpp` | `94fedf21415246219f064c2f1af33448e1245ef1c55e4f69f72275e2b9665c7e` |

验收结果为：L-lite Python 定向测试 33/33，主 L Python 定向测试 37/37；两侧相关 MLIR 测试
均为 7/7；两侧原生库均重建成功。这里的“对齐”只指共同的合法性与物化能力，不表示两个
项目整体相同：L-lite 仍使用穷举原生 autotune，主 L 仍使用 HBV 因果预测、残差、区间和发布
体系。

### 9.5 当前仍未完成的范围

1. 这不是在同一最终源快照下重新执行全部 43 算子的完整性能测量；它是首次 43 算子终态
   账本，加上全部历史受影响角色的最终源码回归；
2. 15 个超时角色没有修复后收益标签，不能把候选级局部进度当作完整性能结果；
3. 272 个历史缺陷单元中，最终时限内实际到达的坐标没有覆盖全部 272 个，因此结论是“已到达
   坐标零复现 + 代表性失败切点已跨越”，不是穷尽证明；
4. 公开仓当前仍以分层 API 和编译器 pass 为主；一键 `candidate → PlanBundle → binding` 的
   CandidateCompiler 产品入口仍是架构设计书中明确的后续工程项。它不影响上述内部测试证据，
   但意味着公开快照尚不能被描述为安装后自动跑完整 43 算子测试的交付产品。
